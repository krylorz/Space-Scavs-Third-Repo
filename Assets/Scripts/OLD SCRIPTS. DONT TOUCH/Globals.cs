using UnityEngine;
using System.Collections;

public class Globals{
	//This class holds a constant global flag for if input is available (used for globally turning on and off input ) ON by default
	public static bool isInputOn = true;
}
